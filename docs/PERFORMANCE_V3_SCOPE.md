# Périmètre préservé

Cette branche ne remplace pas les routes avancées `/`, `/map`, `/social`, `/settings`, `/help`, `/faq`, `/terms`, `/privacy`, `/cookies` ni `/mcp`.

Elle conserve l’identité Global Party, les traductions, le consentement, le centre d’aide, les paramètres et le MCP v2 déjà présents sur `main`.
